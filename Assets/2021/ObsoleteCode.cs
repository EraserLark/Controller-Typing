using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObsoleteCode : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        //Gives 8 directional movement
        float newX = QuantizeAxis(move.x);
        float newY = QuantizeAxis(move.y);
        
        //Normalize this??
        Vector2 m = new Vector2(newX, newY) * moveSpeed * Time.deltaTime;
        transform.Translate(m, Space.World);
        
        if(m.magnitude >= 10)
        {
        
        }
        */
    }
}
